---
name: dev-standard
description: 项目开发与改造规范：先出简短计划确认再动手，改造时规范优先不在错误基础上堆叠，编码遵循项目风格（命名、消灭魔法数字、恰当公共抽取、注释、日志、配置化），表设计优先复用，导出与批处理考虑数据量与安全，交付时必须产出测试文档、完成本地自测并生成测试报告。【触发门禁】本技能仅当用户明确说"使用开发工作流"时才加载；普通的开发新功能、改造、修复缺陷等请求一律不要自动加载本技能。
---

# 项目开发与改造规范

适用场景：在现有项目上开发新功能、改造或修复缺陷。核心原则一句话：**先计划确认，再规范开发，最后测试交付**——不在错误的底子上堆代码，不为完成功能牺牲可维护性。

## 一、动手之前：先出计划

1. **改造或开发前，必须先出一份简短计划**给用户确认。内容包括：打算改哪些文件或位置、每处做什么调整、分几步完成。几行到十几行即可，不累赘——目的是让用户了解改造思路，不是写设计文档。
2. **不确定的绝不擅自改。** 业务规则、现有代码的意图、影响范围拿不准的：
   - 在计划中列为"待确认项"，向用户提出
   - 用户暂时无法回答的，落在一份单独的"待确认清单"文档中，先做确定的部分
3. **先读懂再动手。** 修改前通读目标代码及其调用链，理解现有意图；没读懂就改，等于在错误的基础上继续堆叠。

## 二、改造原则

1. **规范优先，不将就。** 现有代码方法散落、位置不合理、命名混乱的，在完成本次功能的同时把该调整的调整过来（移动位置、改名、拆分方法），并同步修改调用处。不要为了"少动"在错误的结构上堆叠新代码——那样后续维护和审查的成本只会更高。
2. **风格跟随项目。** 动手前观察项目的既有风格：分层方式、命名习惯、返回结构、异常处理、工具类用法。新代码与整体风格一致，不引入第二套风格。
3. **控制影响面。** 调整公共方法或接口时，检查所有调用方并同步修改；确实无法同步的保持向后兼容，并在计划中说明。

## 三、编码规范

1. **命名**：见名知义，遵循项目既有命名惯例；类、方法、变量、实体字段、配置项同样要求。
2. **消灭魔法数字和魔法字符串**：状态码、类型码、阈值等一律定义为有语义的常量或枚举。
3. **公共抽取，但不过度封装**：
   - 两处以上使用、语义相同的逻辑，抽取为公共方法或工具类
   - 语义明显不同的逻辑，不因为"长得像"强行共用
   - 仅一两行代码且无其他调用方的，不封装
4. **注释**：实体字段、关键业务节点、复杂方法必须有注释，说明用途和业务含义（为什么这样做），让不了解这段代码的人能快速上手。
5. **日志**：程序入口打印请求参数，关键节点和出口打印结果，异常必须记录异常栈；禁止空 catch 吞异常；敏感信息（密码、手机号、证件号等）不打进日志。
6. **配置化**：会随环境或运营策略变化的值（地址、开关、阈值、上限、文案）用配置文件、配置中心或字典表管理，不硬编码；确实极少改动的可以硬编码，但加注释说明原因。

## 四、数据表

1. 明显可复用的表，加字段就能满足的，不新增表。
2. 确需新增表时，说明它与现有表的边界；字段命名、类型风格与现有表保持一致。
3. 常作为查询条件的字段考虑索引；不在循环内逐条查询数据库（N+1 问题），改为批量查询。

## 五、性能与安全

1. **数据量意识**：列表、导出、批处理功能必须考虑数据增长。导出类（Excel/CSV）采用分批查询加流式写出，禁止一次性把全量数据加载进内存（防 OOM）。
2. 入参校验与权限校验不因"内部调用"而省略；数据库操作使用参数化，不拼接 SQL。
3. 涉及数据修改的操作考虑失败场景：事务、回滚、重复执行的幂等性。

## 六、交付流程与产出物

按顺序产出以下内容，缺一不可：

1. **改造计划**（动手前）：见"一"，经用户确认后执行。
2. **代码**：遵循上述各条规范。
3. **测试文档**（开发完成后）：按功能列出测试点、操作步骤、预期结果，让其他开发能照着在本地完成测试。
4. **本地自测**：自己先完成测试。本地缺少环境或配置（数据库、中间件、第三方依赖等）时，明确说明缺什么、导致哪些用例未能实际验证——不能用"简单跑了一下"糊弄。
5. **测试报告**：记录测试范围、用例与结果、发现的问题及处理方式，供开发人员查看。

## 七、提交前自查

- 无魔法数字和魔法字符串，命名规范
- 关键位置有注释，入口和异常处有日志
- 可配置项已配置化，保留硬编码处有注释说明
- 公共抽取恰当，无过度封装
- 改动的公共方法已检查全部调用方
- 导出与批处理已考虑数据量
- 测试文档、自测说明、测试报告齐全
- 待确认项已全部列明，没有擅自决定的内容
